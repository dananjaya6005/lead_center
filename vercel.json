{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "public"
        }
      }
    ],
    "scripts": {
      "build": "npm install --legacy-peer-deps && npm run build"
    }
  }
  